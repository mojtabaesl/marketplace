{
  "name": "miniapp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/miniapp/src",
  "projectType": "application",
  "tags": ["scope:miniapp", "type:app"],
  "// targets": "to see all targets run: nx show project miniapp --web",
  "targets": {
    "build": {
      "syncGenerators": [
        "@marketplace/tailwindcss-sync-plugin:update-tailwind-globs"
      ]
    },
    "serve": {
      "syncGenerators": [
        "@marketplace/tailwindcss-sync-plugin:update-tailwind-globs"
      ]
    }
  }
}
